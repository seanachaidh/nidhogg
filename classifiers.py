#We are not going to build our own classifiers but we are going to
#create a framework so that we can easily plug them in trough a common model

class Classifier:
    def __init__(self, name, ):
        pass
    
